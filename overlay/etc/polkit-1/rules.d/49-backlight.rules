polkit.addRule(function(action, subject) {
    if (action.id == "org.xfce.power.backlight-helper" &&
        (subject.user == "root" || subject.user == "ghost")) {
        return polkit.Result.YES;
    }
});
